services:
  - type: web
    name: kidswoertli-ocr
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app --workers=1 --timeout=120"
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
